<?php

namespace Primebeyonder\LaravelApiVisibility\Tests;

use Orchestra\Testbench\TestCase as BaseTestCase;
use Primebeyonder\LaravelApiVisibility\ApiVisibilityServiceProvider;

abstract class TestCase extends BaseTestCase
{
    /**
     * Get package providers.
     *
     * @param  \Illuminate\Foundation\Application  $app
     * @return array
     */
    protected function getPackageProviders($app)
    {
        return [
            ApiVisibilityServiceProvider::class,
        ];
    }

    /**
     * Define environment setup.
     *
     * @param  \Illuminate\Foundation\Application  $app
     * @return void
     */
    protected function defineEnvironment($app)
    {
        // Setup default database to use sqlite :memory:
        $app['config']->set('database.default', 'testbench');
        $app['config']->set('database.connections.testbench', [
            'driver'   => 'sqlite',
            'database' => ':memory:',
            'prefix'   => '',
        ]);

        // Set API Visibility config
        $app['config']->set('api-visibility.allow_in_production', true);
    }
}
